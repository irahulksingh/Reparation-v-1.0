<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title -Hallbergs-Workorder</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Content/NewStyle.css" rel="stylesheet" />
</head>
<body>
   
                <div class="navbar navbar-inverse navbar-fixed-top">
                    @*<div class="container">
                        @*<div class="navbar-header">
                            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>*@
                    @*@Html.ActionLink("Work Order", "", "", new { area = "" }, new { @class = "navbar-brand" })
                        </div>*@
                    @if (Session["UserId"] != null)
                    {
                        <div class="navbar-collapse collapse">
                            <ul class="nav navbar-nav">
                                <li>@Html.ActionLink("All work-orders", "Index", "WorkOrders")</li>
                                <li>@Html.ActionLink("New work-order", "Create", "WorkOrders")</li>
                                @*<li>@Html.ActionLink("Contact", "Contact", "Home")</li>*@
                                <li>@Html.ActionLink("Logout", "Logout", "UserAccount")</li>
                            </ul>
                        </div>

                    }
                </div>
    @if(Session["UserId"] != null)
    {
        <div class ="alert-success">Welcome <b>@Session["FullName"]</b></div>
    }
        @RenderBody()
    <hr />
        
            <footer class="text-center">
                <p>&copy; @DateTime.Now.Year</p>
            </footer>
        
    

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
